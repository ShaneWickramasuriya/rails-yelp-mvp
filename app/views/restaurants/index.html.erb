<div class="container">
    <h1>Restaurants</h1>
    <% @restaurants.each do |restaurant| %>
    <h3><%= link_to restaurant.name, restaurant_path(restaurant) %> - <%= restaurant.category %></h3>
    <h4>----</h4>
    <% end %>
    <h4 class="btn btn-light"><%= link_to "Add a restaurant", new_restaurant_path %></h4>

</div>
